# Google OAuth (for "Sign in with Google")
# Create credentials at https://console.cloud.google.com/apis/credentials
#
# Fix "Error 400: redirect_uri_mismatch":
# 1. Open your OAuth 2.0 Client ID in Google Cloud Console
# 2. Under "Authorized redirect URIs" add the EXACT URL your app uses:
#    - Local:  http://localhost:3000/api/auth/google/callback
#    - Or:     https://your-domain.com/api/auth/google/callback
# 3. No trailing slash. Scheme (http/https) and port must match.
# 4. Optional: set GOOGLE_REDIRECT_URI to that exact URL if the app's origin is wrong (e.g. behind a proxy).
#
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-client-secret
RESEND_API_KEY=re_8YrDcMEL_7VPwm4QE8DxKETQQ7XgJkc82
# GOOGLE_REDIRECT_URI=http://localhost:3000/api/auth/google/callback

# OTP verification (optional for testing)
# If RESEND_API_KEY / Twilio are not set, the 6-digit OTP is only printed in the server
# terminal (where you run npm run dev). Check that terminal for the code when testing.
# Email: Resend free tier https://resend.com (100 emails/day)
# RESEND_API_KEY=re_8YrDcMEL_7VPwm4QE8DxKETQQ7XgJkc82
# RESEND_FROM_EMAIL=onboarding@resend.dev

# SMS: Twilio trial https://www.twilio.com/try-twilio
# TWILIO_ACCOUNT_SID=ACxxxx
# TWILIO_AUTH_TOKEN=your-auth-token
# TWILIO_PHONE_NUMBER=+1234567890

# ── FastAPI Backend (GST Scanner API) ──
# URL of the FastAPI backend. The Next.js frontend calls this for
# invoice processing, order uploads, and JWT auth.
NEXT_PUBLIC_API_URL=http://localhost:8000
